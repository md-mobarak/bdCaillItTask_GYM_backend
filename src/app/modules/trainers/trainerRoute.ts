// import express from 'express';
// // import TrainerController from './controller';
// import { authorize } from '../../middlewares/Auth';
// import trainerController from './trainerController';

// const router = express.Router();

// router.post('/', authorize(['Admin']), trainerController.createTrainer);
// router.get('/', authorize(['Admin']), trainerController.getAllTrainers);
// router.get('/:id', authorize(['Admin']), TrainerController.getTrainerById);
// router.put('/:id', authorize(['Admin']), TrainerController.updateTrainer);
// router.delete('/:id', authorize(['Admin']), TrainerController.deleteTrainer);

// export default router;
